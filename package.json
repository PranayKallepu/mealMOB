{
  "name": "mealmob-monorepo",
  "version": "1.0.0",
  "description": "Root package for MealMOB",
  "private": true,
  "scripts": {
    "start": "npm start --prefix server",
    "install-all": "npm install --prefix server && npm install --prefix client-side"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
